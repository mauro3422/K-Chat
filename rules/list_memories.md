# list_memories
**Lista las entradas guardadas en memoria (memory.db). Puede filtrar por prefijo de key (ej: 'user:', 'bug:', 'proyecto:'). Si no se especifica filtro, muestra todas agrupadas por categoría.**

<!-- auto:params -->
| Parámetro | Tipo | Requerido | Default | Descripción |
|---|---|---|---|---|
| `prefix` | string | No |  | Filtrar por prefijo de key (ej: 'user:', 'bug:', 'proyecto:', 'arquitectura:'). Vacío = todas. |
