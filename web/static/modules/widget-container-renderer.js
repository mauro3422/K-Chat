// Legacy bridge for the old JS modules.
// The real widget container renderer now lives in the TypeScript bundle emitted by Vite.

export { processWidgetContainers } from '/static/dist/assets/widget_container_renderer.js';
