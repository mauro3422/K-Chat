{
  "name": "kairos",
  "version": "0.0.7",
  "private": true,
  "scripts": {
    "lint": "eslint web/static/**/*.js",
    "lint:fix": "eslint web/static/**/*.js --fix",
    "test:js": "vitest run",
    "test:py": "python -m pytest tests/ -v",
    "test": "npm run test:py && npm run test:js && npm run lint"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "vitest": "^4.1.8"
  }
}
