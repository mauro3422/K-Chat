# v0.2.2 - 2026-06-29

## Corregido

- **CI expandido**: El workflow de GitHub Actions ahora corre la suite unit completa (`tests/unit/`) en lugar de solo 3 archivos de smoke tests. Antes: `test_app_factory.py`, `test_anti_regression.py`, `test_web_logging.py` (3 archivos, ~2% del suite). Ahora: 1859 tests unitarios.
- **Dependencias de test faltantes en requirements.txt**: Agregado `pytest-cov` (requerido por `addopts` en `pytest.ini` pero no listado) y `pytest-mock` (usado en multiples tests). Sin estas deps, cualquier `pytest` plano fallaba con "unrecognized arguments".
- **Leader leases stale eliminados**: Borrados 4 archivos de estado expirados:
  - `kairos_memory.db.leader.json` (expirado hace ~213h)
  - `kairos_memory.db.write-queue.json`
  - `kairos_curated_memory.db.leader.json` (expirado hace ~40h)
  - `kairos_curated_memory.db.write-queue.json`

  Verificado que el nodo local sigue healthy tras la limpieza (`/health` returns status ok, lease None, queue 0).

## Documentacion

- **README actualizado**: "16 Tools" a "33 Tools" con lista completa de herramientas. "664 Python + 22 E2E tests" a "1928 Python tests". El conteo anterior estaba desactualizado desde v0.0.52.
- **ROADMAP actualizado**: "16 tools total" a "33 tools total" en la entrada de v0.0.52.
- **docs/ARCHITECTURE_SUMMARY.md actualizado**: Lista de herramientas y conteo actualizado de 16 a 33.
- **docs/tools-architecture.md actualizado**: Total de herramientas auto-descubiertas 16 a 33 (v0.2.0+).
- **Docs de planificacion archivadas**: 3 documentos de planificacion LAN ya completados movidos a `docs/archive/`:
  - `LAN_SYNC_STATUS.md` (plan LAN completado)
  - `IMPLEMENTATION_PLAN_LAN_SYNC.md` (marcado COMPLETADO)
  - `CHECKLIST_PRECONDICIONES_LAN_SYNC.md` (marcado COMPLETADO)
- **docs/REFACTOR_PENDING.md**: Nuevo documento con investigacion completa de dos areas tecnicas pendientes:
  1. `web/routers/node.py` god-router (519 lineas, 14 endpoints) - plan de split en 3 routers + 1 service
  2. TS migration - estado real: source migrado completo (82 archivos .ts), lo que falta es cierre cosmetico (renombrar `app_mock.ts`, migrar `model-availability.js`, alinear README)

## Tests

- `venv/bin/python -m pytest --collect-only -q` -> 1928 tests recolectados, 0 errores
- `venv/bin/python -m pytest tests/unit/ --collect-only -q -o addopts=""` -> 1859 tests en suite unit, 0 errores de import
- `venv/bin/python -m pytest tests/unit/test_app_factory.py -o addopts="--cov=src --cov-report=term-missing" -q` -> 13 passed, coverage funcional
