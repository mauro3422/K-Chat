# v0.0.62 - Stream legacy migrado a TS + debug unificado

> Fecha: 2026-06-17
> Tags: `frontend` `typescript` `migracion` `debug` `stream`
> Tests: `npx vite build` + `npx vitest run --config vitest.ts.config.js` + `pytest --testmon`

## Stream migration (JS → TS bridge shims)

- **stream-dispatcher.js** → bridge shim re-exportando `StreamDispatcher` desde TS
- **stream-fetcher.js** → bridge shim re-exportando `NDJSONStreamClient` desde TS
- **stream-lifecycle.js** → stubs compatibles (funcionalidad inlineada en StreamOrchestrator.ts)
- **content-handler.js** → bridge shim re-exportando `ContentHandler` (eliminado el auto-register de side effect)
- **tool-call-renderer.js** → bridge shim re-exportando `ToolCallRenderer` (eliminado el auto-register)
- **retry-handler.js** → bridge shim re-exportando `RetryController` desde TS
- **stream-orchestrator.js** → bridge shim re-exportando `StreamOrchestrator` desde TS

## Debug unificado

- **log-ui.js** → bridge shim delegando a `LogUI.ts` (mismas exportaciones, 1:1)
- **DebugManager.ts** expandido con features del viejo `debug-panel.js`:
  - Backend log loading + display (`loadBackendLogs()`)
  - API-sourced debug info (modelo, razonamiento, fases, tools, prompt, historial)
  - Stream duration calculation (parsea eventos UI start/complete)
  - Widget DOM state info (iframes, lifecycle events)
  - Copy buttons individuales por sección (stream, UI, widgets, backend)
  - Métodos `setSessionId()`, `loadDebugInfo()`, `loadBackendLogs()` en interfaz

## Fixes críticos

- **reasoning-handler.ts**: Eliminado el auto-register side effect en línea 85 (`registerReasoningHandler()`)
- **widget-container-renderer.ts**: Arreglado constructor que pasaba `{} as any` — ahora crea instancias reales de `IframeBuilder`, `WidgetRegistry`, `WidgetStateManager`
- **chat_ts.html**: Agregado cache buster (`?v=1`) para evitar script stale

## Vite config

- 7 nuevas TS entries en `vite.config.js`: stream_dispatcher, stream_fetcher, content_handler, tool_call_renderer, retry_handler, stream_orchestrator, log_ui
- Creados 7 nuevos archivos `.ts` bridge en `web/static/modules/`
- `vitest.config.js`: agregado alias `/static/dist/assets` → `web/static/dist/assets` para que los shims JS resuelvan en tests

## Infraestructura

- Build (`npx vite build`): ✅ 22 assets, 108ms
- TS tests (`vitest --config vitest.ts.config.js`): **96/96 passed** ✅
- Python tests (`pytest --testmon`): **41/42 passed** (1 pre-existing failure en `test_history_service.py`)
