# v0.0.28 — Audit doc and sidebar click decoupling

## Bug Fixes

### Audit and contracts
- Added `docs/LEGOS_AUDIT.md` as the current decoupling audit and backlog.
- Linked the audit from `docs/CONTRACTS.md` so it is visible from the contract map.

### Frontend decoupling
- Removed the sidebar inline `onclick="loadSession(...)"` hook.
- `web/static/session.js` now handles session selection by delegated click events and imports `loadSession` from `web/static/chat-stream.js`.
- Updated the session test to validate the imported seam instead of the old global callback expectation.

## Tests

- `tests/test-session.test.js`
- `tests/test-debug.test.js`
- `tests/test-chat-stream.test.js`
- `tests/test-frontend-integration.test.js`

## Notes

- This is another compatibility-to-contract cut: one more template-level global was removed from the normal path, while the legacy `window.loadSession` alias remains only as transition support.
