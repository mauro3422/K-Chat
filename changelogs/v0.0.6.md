# v0.0.6 — 2026-06-11

## Agregado

### Calidad de código
- **Ruff 71→0**: todos los errores de linting eliminados (29 unused-import, 19 unused-variable, 9 import-not-at-top, 6 ambiguous-name, 6 f-string sin placeholder, 2 multiple-imports)
- **Pyright 16→0**: todos los errores de type checking eliminados (OpenAI provider, repos, tools, core, llm, cli)
- **Auto-fix**: 16 errores Ruff corregidos automáticamente, 55 manualmente

### Refactorización de monolitos Python
- **`src/core/tool_loop.py`** (314→~250 líneas): extraídas 3 helpers — `_process_stream_event()`, `_execute_tools()`, `_handle_fallback()`; el loop principal quedó como orquestador delgado
- **`src/api.py`** (250 líneas): imports agrupados por fuente (`# --- Core ---`, `# --- LLM ---`, etc.), funciones agrupadas por propósito (`# === Session ===`, `# === Messages ===`, etc.)

### Refactorización de frontend JS
- **`web/static/modules/chat-form.js`** (300→294 líneas): extraídos `abortStreamTimeout()`, `markPillAsError()`, `markCallingPillsError()`, `scheduleRetry()`; eliminada duplicación de patrón de reintento (3→1)
- **`web/static/modules/widgets/toolbar.js`** (286→291 líneas): extraído `createToolbarButton(config)` que factoriza ~80 líneas de estilo duplicado; funciones anónimas reemplazadas por nombradas (17 handlers)

### Tests nuevos
- **45 tests JS nuevos** en 3 archivos:
  - `tests/test-session.js`: 17 tests (rename, cancel, delete, popstate, eventos DOM)
  - `tests/test-debug.js`: 15 tests (logUI, logStream, toggleDebug, copyText, copyWidgetLog)
  - `tests/test-chat-stream.js`: 13 tests (loadSession, replaceState, DOMContentLoaded, widgets reset, form reset)

## Corregido
- `sanitize_widget_id` re-export agregado a `src/api.py` (era importado por `web/routers/widgets.py`)
- Tests JS: mocks de DOM corregidos para `navigator.clipboard`, `window.*` vs `global.*`
- `test-frontend-integration.js`: aserción `esc` → `scrollToBottom` (renombre previo)

## Cobertura
- **Python**: 431 tests (2 flaky pre-existentes)
- **JS**: 110 tests totales (45 nuevos + 65 existentes)
- **Linting**: Ruff 0 errores, ESLint 0 errores
- **Type checking**: Pyright 0 errores
