# v0.0.17 — 2026-06-11

## Arquitectura

### LLM Layer refactor
- `src/llm/models.py` dividido en: `providers.py` (registry + factory), `model_state.py` (cache + fallback + thread-safe), `models.py` (shim backwards-compat)
- `client.py`: dedup de retry logic con `_with_fallback()` y `_extract_debug_usage()`

### Frontend refactor
- `stream-orchestrator.js` dividido en: `stream-fetcher.js` (fetch HTTP + NDJSON) + `stream-retry-coordinator.js` (retry logic centralizado)
- **Bug fix**: `getStreamTimeout()` retornaba `null` → ahora retorna 30000ms (30s default)

### Memory refactor
- `SessionRepository.delete()`: cada repo ahora tiene `delete_by_session(session_id, cursor)`
- Atomicidad con cursor compartido + commit/rollback manual en SessionRepository

### Tools DRY
- `resolve_and_validate_path()` en `_path_helpers.py`: elimina ~15 líneas duplicadas de path resolution en read/write_file
- Eliminada doble llamada a `_validate_path`

### Dead code eliminado
- `run_migrations()` en migrations.py
- `set_engine()` en database.py
- `MIN_RESULTS`, `RETRY_BACKOFF` en web_search.py
- `TypeVar` en api/_repos.py
- `_repo`, `_get_repo()` en history.py

## Frontend

### stream-orchestrator split
- `stream-fetcher.js` (68 líneas): fetch HTTP + NDJSON parsing + status codes
- `stream-retry-coordinator.js` (36 líneas): `attemptRetry()` centralizado
- `stream-orchestrator.js` reducido a orquestador delgado

### Bug fixes
- `getStreamTimeout()`: fallback a 30000ms en vez de null
- 3 fetches sin `.catch()`: messaging.js, toolbar-core.js, toolbar-history.js
- clipboard pattern unificado en debug.js (5→1 función)
- script tags con `defer` en chat.html

### Seguridad
- XSS fix en content-handler.js: fallback usa textContent en vez de innerHTML
- Debug endpoint: solo accesible desde localhost o TESTING env
- N+1 transaction fix en _tool_persister.py: atomicidad corregida

## Infraestructura
- Dockerfile multi-stage con healthcheck
- docker-compose.yml con volume para SQLite
- .pre-commit-config.yaml (ruff, eslint, hooks)
- .dockerignore optimizado
- CI: Ruff + Pyright agregados al workflow
- requirements.txt: versiones pinneadas con upper bounds
- Healthcheck endpoint: GET /health

## Documentation
- CHANGELOG.md: v0.0.8 a v0.0.15 documentadas
- README.md: actualizado a v0.0.15, src/api/ package, Docker
- docs/ARCHITECTURE.md: actualizado con estructura actual

## Tests
- 30 tests nuevos: tool_parser, rate_limiter, history_parser, stream_error_classifier, message_persister
- mock_conn fixture para test_repositories.py
- Lazy lxml en fetch_url.py
- ESLint: parserOptions.sourceType: "module"
