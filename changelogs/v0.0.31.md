# v0.0.31

## Changes

- Removed the empty `stream-renderer.js` loader.
- Removed the template include that loaded the dead module.
- Kept the architecture docs aligned with the smaller runtime surface.

## Validation

- `npm run build`
- `vitest run tests/test-frontend-integration.test.js tests/test-chat-stream.test.js tests/test-session.test.js tests/test-debug.test.js`
