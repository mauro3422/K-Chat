# v0.0.58 — File Attachments + Go Mode Model Selector

## Frontend
- `message-renderer.js` — `_renderFileCard()` y `_renderAttachments()` para mostrar archivos adjuntos en burbujas de chat con iconos por tipo.
- `web/static/styles/messages.css` — Estilos `.file-attach-card` con glassmorphism y hover glow.
- `widgets/canvas-workspace.ts` — Ajustes de estilo.

## Backend
- `web/routers/pages.py` — Model selector ampliado con modo "go": premium, standard y economy models.
- `web/routers/chat.py` — Eliminado `_wrapped_chat_stream` (indirección innecesaria post-OrchestratorDeps facade).

## Tests
- `tests/unit/test_discovery.py`, `tests/unit/test_pages_router.py` — Actualizados para reflejar cambios en model selector.

## Bugfix
- `chat-form.js` — Fix `ReferenceError: text is not defined`. Se lee `input.value` correctamente. Se elimina bloque duplicado de append de mensajes. (commit v0.0.57 + rebuild dist)
