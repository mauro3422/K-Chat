# v0.0.5 — 2026-06-11

## Agregado

### Auditoría de salud completa
- Auditoría de 9 áreas del proyecto: src/tools, src/core, src/llm, src/memory, src/ top-level, web backend, web frontend JS, tests, config/build
- 43 hallazgos documentados con referencias archivo:línea
- 10 fixes aplicados en paralelo usando 5 sub-agentes independientes

### Extracción de repositorios (refactor mayor)
- `src/memory/repos/` con 8 archivos individuales:
  - `base.py`: clase base `_BaseRepository` con `_transaction()` y `_get_conn()`
  - `message_repository.py`: `MessageRecord` + `MessageRepository`
  - `session_repository.py`: `SessionRepository`
  - `tool_call_repository.py`: `ToolCallRepository`
  - `widget_state_repository.py`: `WidgetStateRepository`
  - `debug_repository.py`: `DebugRepository`
  - `saved_widget_repository.py`: `SavedWidgetRepository`
  - `__init__.py`: re-exporta todo + `Repositories` dataclass + `get_repos()`
- Shim de retrocompatibilidad en `repositories.py` (`from .repos import *`)
- 6 repos en 1 archivo → 8 archivos individuales (cohesión mejorada)

### Estandarización de tools
- Formato de retorno unificado a `[OK]`/`[ERROR]` en las 10 tools
- Eliminados prefijos inconsistentes: `Success:`, `[ERROR]:`, `[INFO]`, `[DONE]`
- 23 cambios en 10 archivos de `src/tools/`

### Sanitización frontend
- `KairosUtils.escHtml()` como helper de sanitización de HTML
- innerHTML reemplazado por `textContent` + creación segura de elementos en:
  - `web/static/modules/widgets/iframe.js`
  - `web/static/modules/chat-form.js`
  - `web/static/modules/widgets/toolbar.js`

### CI mejorada
- Stress test `test-stress-stream-abort.js` agregado a `test:js` en `package.json`
- Nuevo step en CI workflow para ejecutar stress test

### TOOLS.md auto-generado
- `load_context()` regenera TOOLS.md desde `TOOL_DEFINITIONS`
- Ya no se mantiene manualmente

## Corregido

### Dead code eliminado
- `src/core/__init__.py`: re-exports muertos de `_deps`, `chat_sync`, `context`, `tools`
- `src/llm/__init__.py`: re-exports muertos de `client`, `models`, `manager`
- `src/llm/models.py`: `_provider_registry` + `register_provider()` (nunca usado)
- `web/routers/pages.py`: endpoint `GET /new-session` sin consumidores frontend

### Bugs
- `src/cli.py`: `session_id` inline con `uuid.uuid4()` → movido a `orchestrator.generate_session_id()`
- `tests/integration/test_api_endpoints.py`: test de endpoint eliminado
- `tests/unit/test_pages_router.py`: assertions de ruta eliminada
- `tests/unit/test_repositories.py`: patch paths actualizados a `src.memory.repos.base.get_conn`
- 7 assertions `"Success"` desactualizadas → `"[OK]"` (por cambio de formato tools)

### Frontend
- `.msg-interim` class CSS sin consumidores eliminada
- `KairosUtils.esc()` renombrado a `KairosUtils.scrollToBottom()` (nombre engañoso corregido)
- `chat-form.js`: escaping mejorado de `<` y `>` a `KairosUtils.escHtml()`

### Dependencias corregidas
- `test_llm.py`: import `from src.llm import chat` restaurado en `__init__.py`
- `test_core.py`: import `from src.core import chat` restaurado en `__init__.py`

## Limpieza
- `validate_findings.py` eliminado (reportaba falsos positivos)
- Documentación actualizada: README, AGENTS.md, ARCHITECTURE.md, MODULES.md, ROADMAP.md

## Arquitectura
- **Sub-agentes paralelos**: 5 agentes ejecutados simultáneamente con cero conflicto de archivos
- **Shim pattern**: `repositories.py` convertido a shim de retrocompatibilidad para refactor gradual
- **`__all__` explícito**: cada archivo de `repos/` define su API pública

## Notas técnicas
- 10 archivos >200 líneas identificados como candidatos a refactor futuro
- 48 errores Ruff (29 unused-import, auto-fixables)
- 15 errores Pyright (concentrados en openai_provider y tools)
- 3 archivos JS sin tests directos: session.js, debug.js, chat-stream.js
- 2 tests flaky pre-existentes en test_web_logging.py (interferencia de logging handlers)
