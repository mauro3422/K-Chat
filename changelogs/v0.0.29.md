# v0.0.29

## Changes

- Split the frontend bundle entry out of `web/static/main.js` into `web/static/app.js`.
- Removed the obsolete `web/static/main.js` wrapper once the bundle entry moved.
- Updated `vite.config.js` so the production build uses the explicit app entry.
- Synchronized the frontend architecture docs and the legos audit with the new boundary.

## Validation

- `npm run build`
- `vitest run tests/test-frontend-integration.test.js tests/test-chat-stream.test.js tests/test-session.test.js`
