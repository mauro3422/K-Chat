# v0.0.33

## Changes

- Removed duplicate bootstrap initialization so `chat-stream.js` owns the runtime setup once.
- Kept the widget and chat-form bootstraps as compatibility globals only.
- Verified the narrower startup path with build and frontend tests.

## Validation

- `npm run build`
- `vitest run tests/test-frontend-integration.test.js tests/test-chat-stream.test.js tests/test-session.test.js tests/test-debug.test.js`
