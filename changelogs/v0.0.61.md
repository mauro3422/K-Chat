# v0.0.61 - Lego Architecture, Model Selector, SSE/WS Streaming, Telegram Rewrite

> Fecha: 2026-06-17
> Tags: `refactor` `streaming` `telegram` `model-selector` `canvas` `typescript`
> Commits: 103 (v0.0.60..c7ffe03)

---

## Agregado

### Model Selector con sistema de tiers
- Selector de modelos agrupado por tiers Go/Free/Zen con estilo visual mejorado.
- Rate limit tracker: solo modelos con sufijo `-free` detectados como rate-limited.
- Ocultamiento de modelos Zen del selector — solo se muestran Go + Free.
- Discovery de modelos Free desde la API de Zen cuando se opera en modo Go.
- Disponibilidad en tiempo real con indicadores visuales (status dots).
- Single-ping availability check para modelos Free (verificación individual por modelo, no one-for-all).
- Dynamic ModelRegistry Lego — sin nombres de modelos hardcodeados.
- 54 tests comprehensivos para ModelRegistry + actualización de anti-regression.

### SSE/WS Streaming en tiempo real
- SSE (Server-Sent Events) real-time event stream para la Web UI.
- Live token streaming vía WebSocket con fallback a HTTP.
- Stream token-by-token: fase-aware live message + markdown inline + flushes optimizados.
- Lazy SSE connect para no bloquear el page load inicial.
- Scroll inteligente: `isNearBottom` previene scroll flash en SSE reload.
- `appendMessage` sin reemplazar todo el DOM + datos completos en SSE.
- Separate flush intervals para Telegram vs WebSocket.
- Persistencia parcial de reasoning/content en error/timeout.
- Continuaciones sin duplicados + timeout/GeneratorExit persist + fallback_text.

### Telegram Bot (rewrite completo)
- Rewrite completo del bot con arquitectura Lego.
- Session dedup, system prompt leaks corregidos, SVG icons, systemd service.
- Integration tests + file logging + documentación.
- Session lookup corregido, tool ID tracking, max turns configurable.
- Historial rebuild con `tool_call_id` y `tool_calls`.
- Persistencia de reasoning + sidebar refresh en tiempo real.
- Captura de `phases_output` para estructura de mensajes web.
- Tool calls inline en un solo mensaje con formato HTML.
- Auto-rename de sesiones Telegram + comando `/sessions`.
- Comando `/delete session` con notificación SSE + sidebar redirect + tests.
- Lookup de sesiones por `telegram_chat_id` (no por nombre, perf).
- 7 nuevos tests de regresión para fixes de Telegram.

### Canvas / Layout System
- CanvasWorkspace desacoplado como Lego completo.
- CardManager recibe DOM containers via `setContainer`, no por constructor.
- Lego Layout System completo: Grid + CanvasOverlay + CSSInjector + AudioBus.

### Frontend TypeScript
- Preparación del frontend TS para conexión real con la IA.
- Ambos frontends funcionando simultáneamente (legacy + TS prototype).
- IA real activada en TS prototype.
- Sistema de síntesis nocturna + sesiones favoritas + poda de DBs.
- Botón "crear sesión" genera ID correctamente + await fix.
- /new y /reset borran mensajes visualmente del chat.
- /new crea sesión nueva + `delete_message` en API client.
- /reset borra mensajes DB misma sesión.
- /clear no va al LLM + logging en `delete_message`.

## Corregido

- 112 test failures pre-existentes arreglados en 16 archivos.
- 22 integration + widget test failures restantes corregidos.
- CSP bloqueaba blob: images en img-src — corregido.
- `ext is not defined` en `_isImageFile` (message-renderer.js).
- `missing await` en `message_renderer.py` y `background_tasks.py`.
- 6 fixes para streaming + markdown (regression).
- Multi-session + tool loop detection + DB error propagation.
- User message save era dead code (wrong indentation en adapter).
- /delete no borraba nada porque msg IDs no se persistían.
- /delete solo borraba el último msg (sobrescribía 'main').
- /delete no salteaba el adapter (nunca borraba la sesión).
- Redirect a la sesión más reciente (no a /) al borrar.
- Widgets no se renderizaban por regex incorrecta en ContentHandler.
- Sistema de auto-inyección de memorias no funcionaba.
- Migration orphan cleanup tolera `memory_index` global.
- Labels de memorias auto-inyectadas + debugger phases.
- Labels de auto-retrieval en inglés + debugger phases.
- Vite base `/static/dist/` para que chunks resuelvan correctamente.
- Delete sesión ahora persiste en backend + recarga desde API.
- Correcciones post-integración con API real.

## Refactor

- **Desacople arquitectónico completo** — 4 Lego blocks + cleanup: fallbacks, tests, deps, protocols.
- **DI refactor LLM layer** + FASE 2.5 hash dedup.
- **Backend Fase 1-3**: migración Lego — composition root + DI.
- **Backend Fase 4**: routers usan `app.state` con fallback a singletons.
- **Web desacople total**: lifecycle + SSE + tests (refactor completo).
- Break tools↔context cycle: arch analysis tools + post-hooks.
- Rewrite ARCHITECTURE.md reflecting v0.0.57 state.

## Tests

- 54 tests: ModelRegistry completo.
- 7 tests: regresión para Telegram fixes.
- Tests para 16 tools core + diseño FASE 5 Proactividad.
- Dual-message renderer + 55 tests de regresión.
- Anti-regression updates + testmon integrado.
- 112 + 22 test failures corregidos en total.

## Herramientas

- `pytest-testmon` agregado + gitignore para testmondata.
- Update agent instructions para opencode CLI + testmon.
- `.gitignore` runtime files (crash_counter, error_context, telegram pid/offset).

## Verificación

- `npm run build` OK.
- `pytest --testmon` OK (tests afectados por cambios).
- Toda la suite de regresión verde.
