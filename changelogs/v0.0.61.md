# v0.0.61 - Migracion base TS del frontend + logs

> Fecha: 2026-06-17
> Tags: `frontend` `typescript` `debug` `logs` `compatibilidad`
> Tests: `npm run build` + `python -m pytest tests/unit/test_anti_regression.py -q`

---

## Cambios

- Se consolidaron mas piezas del frontend sobre TS sin duplicar lo que ya existia.
- Se agrego `web/src_ts/streaming/StreamErrorHandler.ts` y se dejo un puente compatible durante la transición.
- Se agrego `web/src_ts/core/infra/LogUI.ts` como base TS del logger UI, manteniendo el modulo legacy estable.
- Se integro `SystemLogPanel` en el bootstrap TS para la pestaña de logs del sistema.
- Se conecto `kairos:ui-log` para preservar eventos de UI como `tool_error` en el panel TS.
- Se limpiaron tests de regresion obsoletos y se agregaron checks para los puentes nuevos.

## Verificacion

- Build de frontend OK.
- Suite `tests/unit/test_anti_regression.py` OK.
