[pytest]
markers =
    unit: Pruebas unitarias rápidas (mocked, sin DB real)
    integration: Pruebas de integración (DB real, HTTP server)
testpaths = tests/unit tests/integration
# addopts = --cov=src --cov-report=term-missing --cov-report=html:test-results/coverage  # requiere pip install pytest-cov
