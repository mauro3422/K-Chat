> **Last updated:** 2026-06-19 — Added: `src/config_loader.py` dotenv reset helper, `src/api/lifecycle.py` runtime reset helper, `src/context/templates.py` template cache refresh helper, `src/utils/async_utils.py` thread-pool reset/shutdown helper, `src/memory/retrieval/hydrator.py` schema-compatible hydration fallback, `src/llm/providers.py` explicit registry reset helper, `src/llm/container.py` explicit configure/reset helper, `web/services/event_bus.py` explicit reset helper, `web/routers/skills.py` app-state skill registry resolution, `web/app_factory.py` skills registry wired from `src.skills`, `src/gateway.py` reset_gateway_state helper, `src/tools/runner.py` sync/async tool coercion. Updated: `OrchestratorDeps` → 4 sub-dataclasses, `config` param DI in tools/LLM, llm layer map (providers.py deps), tool I/O helpers moved off `asyncio.to_thread`.

# Module Guide

This document maps every module in the system with its single responsibility, its public interface, and what it depends on.

## Layer Map

```
entry/
  cli.py              → CLI entry point (argparse, REPL loop)
  gateway.py          → Multi-service launcher (process orchestration, status, signals)
  web/server.py       → FastAPI entry point (static files, middleware, exception handlers)

api/
  __init__.py         → package marker only
  history_parser.py   → Parse raw DB rows
  history_rebuilder.py→ Rebuild LLM-ready history
  history_ui.py       → UI filtering and tool matching
  session.py          → Session CRUD, renaming, deletion
  session_contract.py  → Session dependency bundle
  widgets.py          → Widget state and saved widget operations
  widgets_contract.py  → Widget dependency bundle
  debug.py            → Debug payload and log snapshots
  debug_contract.py   → Debug dependency bundle
  tools.py            → Tool history/persistence helpers
  removed wrappers     → `history.py`, `models.py`, `llm.py`, `health.py`

core/
  orchestrator.py     → Chat loop, streaming, compression, debug snapshots
  tool_loop.py        → Tool call orchestration (streaming + sync), ToolLoopContext
  history_parser.py   → Parse raw DB rows into message dicts
  history_rebuilder.py→ Rebuild LLM-ready history from DB state
  history_ui.py       → UI filtering + tool/message alignment
  __init__.py         → package marker only

llm/
  protocol.py         → LLMProvider Protocol (runtime-checkable)
  adapters/openai_adapter.py  → OpenAI/OpenCode SDK provider implementation
  model_state.py      → Thread-safe ModelState class (failed/verified/cached models, explicit reset helper)
  container.py        → DI container for LLM services, explicit reset helper
  circuit_breaker.py  → Per-model circuit breaker, explicit reset helper
  rate_limit_state.py → Per-model rate-limit store, explicit reset helper
  providers.py        → Provider registry + explicit reset helper, accepts optional `config` for DI (fallback to DEFAULT_CONFIG)
  api_call.py         → `_api_call()` wrapper with retry
  retry.py            → execute_with_retry() with exponential backoff, accepts optional `config` for DI
  client.py           → chat() and chat_stream() with fallback, tool delta processing
  discovery.py        → Model discovery and verification, accepts optional `config` for DI
  selector.py         → Default model selection
  verifier.py         → Model verification probe
  failover.py         → Model fallback switching
  __init__.py         → package marker only

tools/
  __init__.py         → Auto-loader: TOOLS, TOOL_MAP, TOOL_DEFINITIONS
  runner.py           → Parallel execution (pool), rate limiter (per-session LRU)
  loader.py           → Filesystem scan + dynamic imports
  _path_helpers.py    → Path traversal guard (realpath + commonpath)
  _widget_helpers.py  → Widget ID sanitization + arg validation
  rules/              → 16 tool rule markdown files auto-generated by src/context/tools_docs.py
  *.py                → Individual tools (DEFINITION + run)

memory/
  db_path.py          → Resolves the memory DB path
  engine_state.py     → DatabaseEngine protocol + engine registry
  connection_pool.py  → Thread-local pooled connections and SQLite setup
  memory_pool.py      → Pooled memory DB connections, explicit reset helper
  embeddings/service.py → Lazy embedding model cache, explicit reset helper
  retrieval/reranker.py → Lazy reranker cache, explicit reset helper
  schema.py           → init_db + schema_version bootstrap
  migration_runner.py → pending migration execution
  repos/              → 7 repos: Message, Session, ToolCall, WidgetState, Debug, SavedWidget, MemoryIndex
  migrations.py       → 9 idempotent migrations (001→009)

context/
  __init__.py         → Re-exports load_context, build_system_prompt, _build_tools_md
  builder.py          → System prompt builder, context loader
  crash_recovery.py   → Crash context ingestion and crash-loop guard
  files.py            → Markdown file loader/creator
  templates.py        → Default templates (SOUL, MEMORY, AGENTS), exported cache refresh helper
  tools_docs.py       → TOOLS.md auto-generator (lazy)
  runtime.py          → Runtime context injection + explicit cache reset helper

  web/
  app_factory.py      → FastAPI app factory, registers exception handlers (ServiceException → JSONResponse)
                      → owns HTTP rate-limit store, config cache reset, event bus, skill registry in app.state
  dev_server.py       → Development server runner
  routers/chat.py     → Streaming POST endpoint (ChatPayload, NDJSON, error classification)
  routers/pages.py    → HTML pages, sidebar, message rendering, model selector
  routers/sessions.py → Rename, delete
  routers/widgets.py  → Widget API (WidgetStatePayload, SaveWidgetPayload)
  routers/skills.py   → Skills catalog API (reads registry from app.state)
  routers/logs.py     → Log query endpoints (dynamic log dirs via file_logger helpers)
  routers/debug.py    → Debug info + backend log buffer (_local_only guard)
  routers/health.py   → GET /health (DB, LLM status, uptime)
  routers/asr.py      → Audio/ASR endpoints (WebSocket live chunks + server-side transcription)
  routers/logs.py     → Log query endpoints
  services/chat_stream.py
                      → NDJSON stream generator, auto-rename (background)
  services/message_persister.py
                      → Persists assistant message + debug info
  services/message_renderer.py
                      → Full HTML message list rendering with widget display
  services/stream_error_classifier.py
                      → Classify errors (rate_limit, timeout, network, model, unknown)
  services/loop_detector.py
                      → Detects infinite tool-call loops
  services/file_logger.py
                      → Persistent file logging
  services/event_bus.py
                      → SSE pub/sub bus with injectable instance + reset helper
  services/stream_retry_handler.py
                      → Coordinates stream retry logic
  services/asr_service.py
                      → Audio/ASR processing
  services/stream_contract.py
                      → NDJSON event contract types
  services/widget_contract.py
                      → Widget contract types
  services/model_catalog.py
                      → Model catalog for web UI
  ui_utils.py         → HTML message rendering (phases, reasoning, tool pills)
  logging_handler.py  → BackendLogHandler (ring buffer on kairos.*)

config_loader.py     → Config dataclass, load_config(), dotenv reset helper
constants.py         → Shared policy constants (MAX_TOOL_TURNS, TOOL_OUTPUT_CHUNK_SIZE, LLM_MAX_RETRIES)
compressor.py        → History compression when > 40 msgs / 6k tokens
background_tasks.py  → Auto-rename session via LLM
handler_cli.py       → CLI commands (/model, /clear, /help)
paths.py             → Path constants (DATA_DIR, DB_PATH, STATIC_DIR, etc.)
utils/async_utils.py → Shared thread pool helper with explicit reset/shutdown helpers
```

---

## `src/api/`

**Responsibility:** Domain modules for sessions, messages, widgets, debug, and tools between web and core.

**Public Interface:** Package marker only. Functions are grouped by domain in sub-modules: `messages.py`, `session.py`, `widgets.py`, `debug.py`, `tools.py`, `session_contract.py`, `widgets_contract.py`, `debug_contract.py`.

**Depends on:** `src.memory.repos`, `src.core.orchestrator`, `src.core.history_ui`, `src.core.history_rebuilder`, `src.llm.selector`, `src.llm.model_state`, `src.llm.client`, `src.config_loader`, `src.memory.connection_pool`

---

## `src/api/messages.py`

**Responsibility:** Message write/read operations.

**Public Interface:**
- `save_message_record(record, repos=None) → None`
- `get_session_messages(session_id, limit=500, repos=None) → list`

**Depends on:** `src.memory.repos`

---

## `src/api/session.py`

**Responsibility:** Session CRUD and cascade deletion wiring.

**Public Interface:**
- `ensure_session(session_id, session_repo=None, deps=SessionOpsDeps | None) → None`
- `rename_session(session_id, name, session_repo=None, deps=SessionOpsDeps | None) → None`
- `delete_session(session_id, session_repo=None, repos=None, deps=SessionOpsDeps | None) → None`
- `get_sessions(limit=50, session_repo=None, deps=SessionOpsDeps | None) → list`

**Depends on:** `src.api.session_contract`, `src.memory.connection_pool`, `src.memory.repos`

---

## `src/api/session_contract.py`

**Responsibility:** Optional dependency bundle for session operations.

**Public Interface:**
- `SessionOpsDeps` — dataclass for session repo / connection injection

**Depends on:** stdlib only

---

## `src/api/widgets.py`

**Responsibility:** Widget state and saved widget operations.

**Public Interface:**
- `save_widget_state(session_id, widget_id, state, widget_state_repo=None, deps=WidgetOpsDeps | None) → None`
- `get_widget_states(session_id, widget_state_repo=None, deps=WidgetOpsDeps | None) → dict[str, str]`
- `db_save_widget(session_id, widget_id, code, description="", saved_widget_repo=None, deps=WidgetOpsDeps | None) → dict[str, Any]`
- `db_get_widget(widget_id, saved_widget_repo=None, deps=WidgetOpsDeps | None) → dict[str, Any] | None`
- `db_get_widget_versions(widget_id, saved_widget_repo=None, deps=WidgetOpsDeps | None) → list[dict[str, Any]]`
- `db_get_widget_by_version(widget_id, version, saved_widget_repo=None, deps=WidgetOpsDeps | None) → dict[str, Any] | None`

**Depends on:** `src.api.widgets_contract`, `src.memory.repos`

---

## `src/api/widgets_contract.py`

**Responsibility:** Optional dependency bundle for widget state and saved-widget operations.

**Public Interface:**
- `WidgetOpsDeps` — dataclass for widget repo injection

**Depends on:** stdlib only

---

## `src/api/exceptions.py`

**Responsibility:** Framework-agnostic exception for the API layer, replacing `HTTPException` to avoid coupling to FastAPI.

**Public Interface:**
- `ServiceException(status_code: int, detail: str)` — raised by domain modules; translated to `JSONResponse` by the web layer's exception handler.

**Depends on:** stdlib only

---

## `src/api/debug.py`

**Responsibility:** Debug payload persistence and ASR telemetry.

**Public Interface:**
- `save_debug_info(session_id, data, debug_repo=None, deps=DebugOpsDeps | None) → None`
- `get_debug_info(session_id, debug_repo=None, deps=DebugOpsDeps | None) → dict[str, Any]`
- `append_asr_telemetry(session_id, event, debug_repo=None, deps=DebugOpsDeps | None) → None`

**Depends on:** `src.api.debug_contract`, `src.memory.repos`

---

## `src/api/debug_contract.py`

**Responsibility:** Optional dependency bundle for debug persistence.

**Public Interface:**
- `DebugOpsDeps` — dataclass for debug repo injection

**Depends on:** stdlib only

---

## Removed wrappers

These transitional modules were deleted and should not be used by new code:

- `src/api/history.py`
- `src/api/models.py`
- `src/api/llm.py`
- `src/api/health.py`

Use the direct modules instead:

- `src.core.history_rebuilder`
- `src.core.history_ui`
- `src.llm.selector`
- `src.llm.model_state`
- `src.llm.client`
- `src.core.orchestrator`
- `src.config_loader`
- `src.memory.connection_pool`

---

## `src/core/debug_info.py`

**Responsibility:** Structured debug metadata replacing the mutable dict bag.

**Public Interface:**
- `DebugInfo` — dataclass with typed fields: `model`, `session_id`, `reasoning`, `tool_calls`, `history_before`, `system_prompt`, `prompt_tokens`, `completion_tokens`, `total_tokens`, `phases`
- `to_dict() → dict` — serialization for DB persistence

**Depends on:** stdlib only

---

## `src/core/history_contract.py`

**Responsibility:** Optional dependency bundle for history reconstruction.

**Public Interface:**
- `HistoryRebuildDeps` — dataclass for history repo injection

**Depends on:** stdlib only

---

## `src/core/orchestrator.py`

**Responsibility:** The conversation engine. Manages the full lifecycle from user input to final response.

**Public Interface:**
- `chat_stream(message_user, history, model, session_id, tagged, debug, phases_output, streaming, ..., deps=OrchestratorDeps | None) → Generator`
- `_compress_if_needed(history, model) → list`
- `_save_debug_info(session_id, model, history_before, chain, system_prompt, tool_calls)`

**Depends on:** `src.core.orchestrator_contract`, `src.llm.client`, `src.tools`, `src.context`, `src.compressor`, `src.memory.repos`

**Key Internals:**
- Delegates tool loops to `tool_loop.run_tool_loop_streaming()` / `run_tool_loop_sync()`.

---

## `src/core/orchestrator_contract.py`

**Responsibility:** Dependency injection contracts for orchestration. `OrchestratorDeps` is a backward-compatible facade that composes 4 focused sub-dataclasses.

**Public Interface:**
- `LLMDeps` — model selection, LLM client functions, telemetry
- `ToolDeps` — tool registry, execution service
- `StorageDeps` — repositories, history service, compression
- `RequestStateDeps` — per-request state (session, debug, background tasks)
- `OrchestratorDeps` — facade: accepts all fields from the 4 sub-groups as keyword args, delegates each to the appropriate sub-dataclass. Supports attribute access and `__init__` with any combination of fields.

**Sub-group access:**
```python
deps.llm        # → LLMDeps
deps.tools      # → ToolDeps
deps.storage    # → StorageDeps
deps.state      # → RequestStateDeps
deps.repos      # → StorageDeps.repos (bridge access)
deps.session_id # → RequestStateDeps.session_id (bridge access)
```

**Depends on:** stdlib only

---

## `src/core/tool_loop.py`

**Responsibility:** Tool call orchestrator — manages reasoning → tool → response cycles (max 25 turns).

**Public Interface:**
- `run_tool_loop_streaming(...) → Generator` — streaming path
- `run_tool_loop_sync(...) → tuple[str, list]` — synchronous path (CLI)

**Key Internals:**
- `_ToolLoopContext` dataclass: wraps 11 parameters into a single object.
- `_process_tool_turn(...)`, `_process_sync_turn(...)`: single-turn processor.
- `_process_llm_stream(...)`: reads LLM stream, yields content/reasoning/tool_calls.
- `_yield_stream_fallback(...)`: fallback when streaming fails.

**Depends on:** `src.llm.client`, `src.tools.runner`, `src.memory.repos`

## `src/constants.py`

**Responsibility:** Shared policy constants imported by feature modules.

**Public Interface:**
- `MAX_TOOL_TURNS`
- `TOOL_OUTPUT_CHUNK_SIZE`
- `LLM_MAX_RETRIES`

**Depends on:** `src.config_loader`

---

## `src/core/history_parser.py`

**Responsibility:** Parse raw DB rows into message lists.

**Depends on:** `src.context`

---

## `src/core/history_rebuilder.py`

**Responsibility:** Rebuild LLM-ready history from parsed rows.

**Public Interface:**
- `rebuild_history(session_id, model, messages_repo=...) → list` — reconstructs LLM-ready history from DB

**Depends on:** `src.memory.repos`, `src.context`, `src.core.history_parser`

---

## `src/core/history_ui.py`

**Responsibility:** UI filtering and tool/message alignment.

**Public Interface:**
- `filter_messages_for_ui(raw_msgs) → list`
- `match_tools_to_msgs(msgs, all_tools) → dict`

**Depends on:** `src.memory.repos`

---

## `src/llm/protocol.py`

**Responsibility:** LLM Provider Protocol definition.

**Public Interface:**
- `class LLMProvider(Protocol)` — `chat()`, `chat_stream()`, `list_models()`

**Depends on:** nothing

---

## `src/llm/adapters/openai_adapter.py`

**Responsibility:** OpenAI/OpenCode SDK adapter.

**Public Interface:**
- `class OpenAIAdapter` — implements `LLMProvider`
- `OpenAIAdapter.list_models() → list[str]`
- registration happens through `src.llm.adapters`

**Depends on:** `src.config_loader`, `src.llm.protocol`, `src.llm.adapters`

---

## `src/llm/api_call.py`

**Responsibility:** `_api_call()` with retry.

**Public Interface:**
- `_api_call(**kwargs)` — wrapper with exponential backoff via `execute_with_retry()`

**Depends on:** `src.llm.providers`, `src.llm.retry`

---

## `src/llm/client.py`

**Responsibility:** Mid-level LLM interface. Coordinates model fallback, streaming token handling, tool delta processing.

**Public Interface:**
- `chat(messages, model, **kwargs) → Choice` — single response
- `chat_stream(messages, model, reasoning_output, tagged, tool_calls_output, **kwargs) → Generator`
- `_update_system_prompt(messages, model)` — refreshes system prompt on model switch

**Depends on:** `src.llm.api_call`, `src.llm.model_state`, `src.context`

## `src/tools/__init__.py`

**Responsibility:** Tool auto-discovery and registration.

**Public Interface:**
- `TOOLS` — list with single `execute_action` schema for LLM API
- `TOOL_MAP` — dict mapping action_name → run function
- `TOOL_DEFINITIONS` — dict mapping action_name → full DEFINITION dict

**Depends on:** `src.tools.loader` (invoked at module init)

---

## `src/tools/runner.py`

**Responsibility:** Execute tool calls in parallel and yield streaming events.

**Public Interface:**
- `run_parallel_tools(tool_calls, session_id, turn, history, tool_detail, used_tools, phase_tool_ids, tagged, tool_map) → Generator`

**Internals:**
- `_session_rate` — dict with per-session rate limiting (30 calls / 10s window, LRU eviction)
- `_rate_lock` — threading.Lock for thread-safe rate checking

**Depends on:** `src.memory.repos`

---

## `src/memory/types.py`

**Responsibility:** Shared types module. Canonical definition of `MessageRecord` — the explicit contract for message persistence used by `save_message_record()`.

**Public Interface:**
- `MessageRecord` — dataclass with fields: `session_id`, `role`, `content`, `model`, `reasoning`, `phases`, `prompt_tokens`, `completion_tokens`, `total_tokens`, `tool_calls`, `tool_call_id`

**Depends on:** stdlib only

**Note:** Re-exported from `src.memory.repos` for transition support; new code should import from `src.memory.types` directly.

---

## `src/memory/schema.py`

**Responsibility:** Schema initialization and version bootstrap.

**Public Interface:**
- `init_db()` — creates tables, ensures `schema_version`, delegates pending migrations, enables foreign keys

**Depends on:** `src.memory.connection_pool`, `src.memory.lifecycle`, `src.memory.migration_runner`, `src.memory.migrations`

## `src/memory/migration_runner.py`

**Responsibility:** Execute pending schema migrations and persist `schema_version`.

**Public Interface:**
- `run_pending_migrations(conn, engine, migrations, current_version, schema_table="schema_version") → None`

**Depends on:** stdlib only

## `src/memory/lifecycle.py`

**Responsibility:** Tracks which database paths are initialized.

**Public Interface:**
- `mark_initialized(db_path)` — record an initialized path
- `is_initialized(db_path)` — check whether a path is already bootstrapped
- `ensure_initialized(db_path, initializer)` — run initializer once per path

**Depends on:** stdlib only

## `src/memory/migrations.py`

**Responsibility:** Schema migrations (idempotent, sequential).

**Migrations (9 total):**
- `_migration_001_initial_schema` → `_migration_009_add_indexes`
- Each uses `IF NOT EXISTS` / `try/except OperationalError` for idempotency
- Last three: add FK references + indexes on all `session_id` columns

**Depends on:** `src.memory.connection_pool`, `src.memory.schema`

---

## `src/context/` (package)

**Responsibility:** System prompt assembly and context file management.

**Public Interface:**
- `build_system_prompt(model) → dict` — `{role: "system", content: ...}`
- `load_context() → str` — reads SOUL + MEMORY + AGENTS from disk and concatenates them
- `_build_tools_md() → str` — generates TOOLS.md from TOOL_DEFINITIONS (lazy import of `src.tools`)

**Depends on:** `src.tools` (for TOOL_DEFINITIONS), `src.paths`, `src.config_loader`

---

## `web/routers/chat.py`

**Responsibility:** The main streaming endpoint.

**Public Interface:**
- `POST /chat/{session_id}` — NDJSON stream (accepts `ChatPayload`: message, model, tagged)

**Internals:**
- Validates input via Pydantic `ChatPayload`
- Rebuilds history from DB via `src.core.history_rebuilder`
- Delegates to `services/chat_stream.py` for NDJSON generation
- Classifies errors (rate_limit, timeout, network, model, empty_response)
- Saves assistant message + debug info + triggers auto-rename

**Depends on:** `src.api`, `src.background_tasks`, `web.services.chat_stream`, `web.services.stream_error_classifier`

---

## `web/routers/pages.py`

**Responsibility:** All HTML page routes.

**Public Interface:**
- `GET /` — new chat page
- `GET /sessions/{session_id}` — existing chat page
- `GET /sidebar` — session list for sidebar
- `GET /sessions/{session_id}/messages` — message list via `message_renderer`

**Depends on:** `src.api`, `web.services.message_renderer`, `web.ui_utils`

---

## `web/routers/sessions.py`

**Responsibility:** Session CRUD.

**Public Interface:**
- `POST /sessions/{session_id}/rename` — rename session
- `DELETE /sessions/{session_id}` — delete session (cascades all data)

**Depends on:** `src.api`

---

## `web/routers/widgets.py`

**Responsibility:** Widget state and versioned widget API.

**Public Interface:**
- `POST /sessions/{session_id}/widgets/{widget_id}/state` — save widget state (`WidgetStatePayload`)
- `GET /sessions/{session_id}/widgets/{widget_id}/code` — get widget code
- `POST /sessions/{session_id}/widgets/{widget_id}/save` — save official widget (`SaveWidgetPayload`)
- `GET /sessions/{session_id}/widgets/{widget_id}/versions` — list versions
- `GET /sessions/{session_id}/widgets/{widget_id}/versions/{version}/code` — get specific version
- All widget endpoints are scoped under `/sessions/{session_id}/widgets/`

**Depends on:** `src.api`

---

## `web/routers/debug.py`

**Responsibility:** Debug information and backend log capture.

**Public Interface:**
- `GET /sessions/{session_id}/debug` — debug snapshot JSON
- `GET /debug/backend-logs` — recent backend log entries

**Internals:**
- `_local_only` guard (skipped when `TESTING=true`)
- `BackendLogHandler` in `web/logging_handler.py`: captures `kairos.*` logger into a ring buffer

**Depends on:** `src.api`, `web.logging`

---

## `web/services/chat_stream.py`

**Responsibility:** NDJSON stream generation.

**Public Interface:**
- `build_stream_generator(session_id, message, history, model, background_tasks, ..., deps=StreamGeneratorDeps | None) → Callable`
- `_classify_error(e) → str` — error type classification (moved to `stream_error_classifier.py`)
- `_handle_token(...)` — accumulates token stream, yields typed NDJSON events

**Depends on:** `src.core.orchestrator`, `src.background_tasks`, `web.services.chat_stream_contract`, `web.services.loop_detector`, `web.services.message_persister`, `web.services.stream_error_classifier`, `web.services.stream_retry_handler`, `web.services.stream_contract`, `web.services.stream_state`

---

## `web/services/chat_stream_contract.py`

**Responsibility:** Optional dependency bundle for backend chat streaming.

**Public Interface:**
- `StreamGeneratorDeps` — dataclass for chat stream hook injection

**Depends on:** stdlib only

---

## `web/services/stream_state.py`

**Responsibility:** Accumulated assistant text and persistence timing for NDJSON streams.

**Public Interface:**
- `StreamState` — dataclass for accumulated stream state

**Depends on:** stdlib only

---

## `web/services/message_renderer.py`

**Responsibility:** Full HTML message list rendering.

**Public Interface:**
- `render_session_messages(session_id, deps=None) → str` — returns complete HTML for all session messages

**Internals:**
- Loads all messages + tool calls, matches tools to messages, renders with phases and widget pills
- Optional dependency bundle lives in `web/services/message_renderer_contract.py`

**Depends on:** `src.api`, `web.ui_utils`, `web.services.message_renderer_contract`

---

## `web/services/message_persister.py`

**Responsibility:** Final assistant message persistence for web streams.

**Public Interface:**
- `save_assistant_message(session_id, full_content, full_reasoning, phases_output, debug_info, model, deps=None) → None`

**Internals:**
- Deduplicates phases, serialises phases JSON, persists assistant message, then writes debug info
- Optional dependency bundle lives in `web/services/message_persister_contract.py`

**Depends on:** `src.api.messages`, `src.api.debug`, `src.memory.repos`, `web.services.message_persister_contract`

---

## `web/ui_utils.py`

**Responsibility:** HTML rendering primitives for messages.

**Public Interface:**
- `render_msg_with_phases(msg, tool_match) → str` — full HTML for a single message
- Individual phase renderers (reasoning with toggle, content, tool pills)

---

## Dependency Direction

```
entry (cli.py, web/server.py)
  ↓
api modules (src/api/*)
  ↓
core (orchestrator, tool_loop, history)
  ↓
llm + tools + memory + context
  ↓
config (config_loader.py, paths.py, .env)
```

The web layer depends on domain modules under src/api. Those modules depend on core, memory, tools, or context as needed. Core depends on llm, tools, memory, context. All depend on config. There are no circular dependencies in runtime.
